language: python

cache: pip

python:
  - "3.7"

before_install:
  - export BOTO_CONFIG=/dev/null

install:
    - pip install -U -r requirements.txt

script:
    - scripts/test

after_script:
- codecov
